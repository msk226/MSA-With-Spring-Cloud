server:
  port: 8888

spring:
  application:
    name: config-service
  profiles:
    active: native
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
  cloud:
    config:
      server:
        native:
          search-locations: file:///Users/minseokkim/native-local-repo
#        git:
#          uri: file:///Users/minseokkim/git-local-repo
management:
  endpoints:
    web:
      exposure:
        include: health, busrefresh